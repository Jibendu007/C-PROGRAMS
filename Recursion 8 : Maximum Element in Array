#include <stdio.h>
int main() {
	int i,j,n;
  scanf("%d",&n);
  int a[n];
  int b;
  b = 0;
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  
  for (i = 1; i < n; ++i)
  {
        if (a[0] < a[i])
            a[0] = a[i];
    }
  printf("Maximum element in the array is %d",a[0]);
	return 0;
}
