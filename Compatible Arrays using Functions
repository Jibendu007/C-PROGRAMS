#include <stdio.h>
int main() {
	int n,i,t;
  t=0;
  scanf("%d",&n);
  int a[n];
  int b[n];
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    scanf("%d",&b[i]);
  }
  for(i=0;i<n;i++)
  {
    if(a[i]>=b[i])
      continue;
    else
      t++;
  }
  if(t==0)
    printf("Compatible");
  else 
    printf("Incompatible");
     
	return 0;
}
